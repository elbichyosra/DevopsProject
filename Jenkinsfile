pipeline {
    agent any
 stages {

     stage('Clean and compile with Maven') {
            steps {
            
                    sh 'mvn clean compile'
                }
            }
    }
}

